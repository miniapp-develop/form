<view class="mini-input mini-class">
    <view class="form-name">{{item.name}}</view>
    <input class=""
           value="{{valueView}}"
           type="{{type}}"
           bind:input="onChanged"/>
</view>