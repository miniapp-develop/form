<view>
    <view>{{item.name}}</view>
    <view>
        <view wx:for="{{item.value}}" wx:key="value" wx:for-item="file">
            <image class="img" src="{{file.src}}"/>
        </view>
    </view>
</view>